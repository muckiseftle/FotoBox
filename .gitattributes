# Normalize all text files to LF in the repository (cross-platform consistency).
* text=auto eol=lf

# Shell scripts and unit files MUST be LF or they break on Linux/macOS.
*.sh text eol=lf
*.service text eol=lf

# Binary assets
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.webp binary
